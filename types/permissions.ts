// This is going to be as painful as Audit Logs... isn't it...

export namespace Permissions {
  // deno-lint-ignore no-empty-enum
  export const enum Flags {
    // TODO(@zorbyte): Write all of these.
  }

  export type Raw = `${number}`;
}
